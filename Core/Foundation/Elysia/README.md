# Elysia Logs

Raw growth telemetry (world_events, symbol episodes, etc.) lives here.
Real runs should append to the JSON/JSONL files listed in README, but the repository only keeps empty placeholders so the directory structure is tracked.

